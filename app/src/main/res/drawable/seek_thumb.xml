<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <size
        android:height="45dp"
        android:width="20dp"
        />
    <solid android:color="@color/white"/>

    <stroke
        android:width="1dp"
        android:color="@color/seek_bar_progress_end"/>

    <!-- 添加圆角属性 -->
    <corners
        android:radius="8dp"/>
</shape>
